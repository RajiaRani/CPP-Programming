#include<iostream>
using namespace std;
bool check(int x,int y);
int main()
{
	if (check(8,7))
	cout<<"both the value are exact";
	else
	cout<<"both the value are wrong";
	return 0;
}
bool check(int x,int y)
{
	if (x<8,y=8)
	return true;
	else
	return false;
}