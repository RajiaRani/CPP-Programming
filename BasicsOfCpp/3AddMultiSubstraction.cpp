#include<iostream>
#include<math.h>
using namespace std;
int main()
{
	//take any two or more numbers
	int a=40,b=60,c=98;
	//subtract the numbers
	int substract =(a - b - c);
	//add the numbers
	int sum = ( a + b + c);
	//multiply the numbers
	int multiply = ( a * b * c);
	cout<<"The substraction is : "<<substract<<endl;
	cout<<"The sum is : "<<sum<<endl;
	cout<<"The multiplication is : "<<multiply<<endl;
	
	return 0;
	
}